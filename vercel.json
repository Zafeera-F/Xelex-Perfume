{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://REPLACE_WITH_RENDER_BACKEND_URL/api/:path*" },
    { "source": "/uploads/:path*", "destination": "https://REPLACE_WITH_RENDER_BACKEND_URL/uploads/:path*" }
  ]
}
